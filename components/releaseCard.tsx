import type { ReleaseModel } from "../util/models/release";

export async function ReleaseCardComponent({ release }: { release: ReleaseModel }) {
    return (
        <div className="container">

        </div>
    );
}